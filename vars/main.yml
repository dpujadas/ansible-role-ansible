---
# vars file for ansible

ansible_role_required_packages:
  - build-essential
  - python-dev
  - libssl-dev

ansible_role_pip_package:
  - name: 'ansible'
    version: '{{ ansible_role_version|d(omit) }}'
