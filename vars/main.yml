---
# vars file for ansible

ansible_role_required_packages:
  - python-dev
  - python-pip
  - libffi-dev
  - libssl-dev

ansible_role_extra_pip_packages: []

ansible_role_aws_pip_packages:
  - boto
  - boto3

ansible_role_docker_pip_packages:
  - docker-py
